// const { Country, User, Merchant, Order } = require('../backend/database/models'); // Adjust the path to your models

// async function clearTables() {
//   try {
//     // Ensure deletion of all associated records first
//     await Order.destroy({ where: {} }); // Assuming there's an Order model
//     await User.destroy({ where: {} });
//     await Merchant.destroy({ where: {} });
//     await Country.destroy({ where: {} });

//     console.log('All records have been deleted.');
//   } catch (error) {
//     console.error('Error deleting records:', error);
//   }
// }

// clearTables();
